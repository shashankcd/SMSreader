name: Android Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build APKs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      # Set up Android SDK
      - name: Set up Android SDK
        uses: android-actions/setup-android@v2
        with:
          api-level: 34
          build-tools: "34.0.0"

      # Generate Gradle Wrapper dynamically
      - name: Generate Gradle Wrapper
        run: |
          mkdir -p gradle/wrapper
          curl -s https://services.gradle.org/distributions/gradle-8.3-bin.zip -o gradle-8.3-bin.zip
          unzip -q gradle-8.3-bin.zip -d gradle/
          echo "d
